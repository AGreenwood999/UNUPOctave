# analysis

Some functions to help analyzing data in octave. 

To load the functions and make them available you need to 'addpath'

So if I want to add all functions in 'fitting', i would run:

addpath("/path/to/analysis/directory/fitting")

and then all fitting functions would be available
